version: '3.8'

services:
  build-and-baraka-vitrine:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    container_name: build-and-baraka-vitrine